
# tabtab source for serverless package
# uninstall by removing these lines
[ -f ~/.config/tabtab/serverless.bash ] && . ~/.config/tabtab/serverless.bash || true

# tabtab source for sls package
# uninstall by removing these lines
[ -f ~/.config/tabtab/sls.bash ] && . ~/.config/tabtab/sls.bash || true

# tabtab source for slss package
# uninstall by removing these lines
[ -f ~/.config/tabtab/slss.bash ] && . ~/.config/tabtab/slss.bash || true
